package org.jobhearted.crawler.processing.objects;

/**
 * Interface for all objects that can have a location added to it.
 */
public interface Locatable {
    public void addLocation(Location location);
}
